const Book = require("../../models/bookmodel");

async function bookDetails(req,res) {
    try {
        
    } catch (error) {
        console.log(error);
        
    }
}

module.exports = bookDetails;